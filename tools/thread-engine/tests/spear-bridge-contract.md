# Spear Bridge Test Contract

Gate 7G-A tests cover the disabled compile-only Spear-to-Weave bridge. Tests must prove package manifest exactness, every carrier entry classification including directory and nonregular-entry rejection, duplicate JSON-key rejection, path rejection, payload/source hash derivation, production mission validation, disabled posture, adapter-ready output containing mission JSON, compile receipt JSON, and exact `PAYLOADS/`, output-symlink rejection, internally owned clone disposal, Windows-safe bounded permission remediation and retry, persistent-disposal fail-closed behavior, exact owned-root cleanup restriction, transactional no-partial-output rejection behavior, no adapter invocation, no `gh`, no write-capable Git command templates, and exact stop at compiled mission output.
